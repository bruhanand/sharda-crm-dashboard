# Deployment Configuration Template
# Copy this to .env.deploy and fill with your actual credentials
# NEVER commit .env.deploy to version control

# ============================================
# Digital Ocean Droplet Configuration
# ============================================

# Droplet IP address
DEPLOY_DROPLET_IP=your_droplet_ip_here

# SSH User (typically 'root' for Digital Ocean droplets)
DEPLOY_SSH_USER=root

# SSH Authentication Method
# RECOMMENDED: Use SSH key authentication
DEPLOY_SSH_KEY_PATH=~/.ssh/id_rsa

# Alternative: Password authentication (NOT RECOMMENDED - use SSH keys instead)
# Only uncomment if you cannot use SSH keys
# DEPLOY_SSH_PASS=your_secure_password_here

# ============================================
# Usage Instructions
# ============================================
#
# 1. Copy this file:
#    cp .env.deploy.example .env.deploy
#
# 2. Fill in your actual credentials in .env.deploy
#
# 3. Load environment variables before running deployment:
#    # Linux/Mac:
#    source .env.deploy
#    ./deploy-digitalocean.sh
#
#    # Windows PowerShell:
#    Get-Content .env.deploy | ForEach-Object {
#        if ($_ -match '^([^#][^=]+)=(.*)$') {
#            [Environment]::SetEnvironmentVariable($matches[1], $matches[2])
#        }
#    }
#
# 4. NEVER commit .env.deploy to version control
#    (it's already in .gitignore)
#
# ============================================
# Security Best Practices
# ============================================
#
# - Use SSH key authentication instead of passwords
# - Rotate credentials regularly
# - Use different credentials for different environments
# - Consider using a secrets manager (AWS Secrets Manager, HashiCorp Vault, etc.)
# - Enable 2FA on your Digital Ocean account
