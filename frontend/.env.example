# API Base URL
# For local development, this should point to your local backend
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Note: Vite requires the VITE_ prefix for environment variables
# These variables will be available in your React code via import.meta.env.VITE_API_BASE_URL
